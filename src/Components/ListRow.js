import React, {Component} from 'react';
import {StyleSheet, View, Text, TouchableOpacity} from 'react-native';

const renderItemListRow = () => {
  return (
    <TouchableOpacity>
      <Text>long</Text>
    </TouchableOpacity>
  );
};

const styles = StyleSheet.create({
  box: {},
});
